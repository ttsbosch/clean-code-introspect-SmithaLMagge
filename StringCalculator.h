#include <iostream>
#include <string>
#include <sstream>
#include <cctype>
using namespace std;

class StringCalculator {
private:
int sumIntegersInString(string str);
public:
int add(string str);
};
